# ---- 核心框架 ----
django
djangorestframework
django-filter
django-cors-headers
python-dotenv

# ---- 数据库 ----
# psycopg2-binary
mysqlclient
pymysql

# ---- 静态文件部署 ----
whitenoise

# ---- 图片 & 文件 ----
# Pillow

# ---- 认证 / 安全 ----
djangorestframework-simplejwt
PyJWT

# ---- 异步任务 & 缓存 ----
celery
redis                          # Celery broker / cache 都能用
# django-redis                 # 如果想把 Django cache 配到 Redis

# ---- 部署相关（Linux 服务器常用）----
gunicorn                       # WSGI 服务器，配 Nginx 用
uvicorn[standard]              # 做 ASGI / Django Channels 时用
django-environ               # 用 env 管理配置的另一种写法

# ---- API 文档 ----
drf-spectacular
# drf-yasg

# ---- 工具库 ----
requests
python-dateutil
ipython
django-extensions

# ---- 日志增强（可选）----
loguru
